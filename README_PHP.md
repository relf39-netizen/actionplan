# คู่มือการติดตั้งและใช้งานบน PHP Web Server (Hosting / cPanel / DirectAdmin / XAMPP)

ระบบแผนปฏิบัติการประจำปีและจัดสรรงบประมาณโรงเรียน (ฉบับ Native PHP 8.x)

---

## 📌 สาเหตุที่เปิดหน้าเว็บไม่ได้ในตอนแรก:
ก่อนหน้านี้โค้ดหน้าบ้านเขียนด้วย TypeScript / React (`.tsx`) ซึ่งเว็บบราวเซอร์และ PHP Web Server (เช่น Apache, LiteSpeed, Nginx) **ไม่สามารถอ่านหรือรันไฟล์ `.tsx` ได้โดยตรง** หากไม่มีการ Build หรือไม่มี Node.js รันอยู่เบื้องหลัง

---

## ✅ สิ่งที่ปรับปรุงและเปลี่ยนเป็น PHP 100% แล้ว:
ตอนนี้ระบบถูกพัฒนาเป็น **Native PHP ล้วน** เรียบร้อยแล้ว:
1. **มี `index.php` อยู่ที่โฟลเดอร์หลัก (Root)** เมื่อเปิดโดเมนหรือโฟลเดอร์ ระบบจะเปิดหน้าเว็บได้ทันที
2. ทุกหน้าทำงานด้วยภาษา PHP + HTML + Tailwind CSS (ผ่าน CDN ไม่ต้องติดตั้ง Node.js หรือ npm ใดๆ ทั้งสิ้น)
3. **ระบบผู้ช่วยเขียนโครงการด้วย AI (`ai_project_writer.php`)** ทำงานด้วย PHP cURL เชื่อมต่อ Google Gemini API และดาวน์โหลดเป็นเอกสาร Word (`.doc`) และพิมพ์เป็น PDF ได้ทันที
4. มีระบบ **Offline / Demo Mode**: แม้ยังไม่ได้ต่อฐานข้อมูล MySQL ระบบก็เปิดหน้าเว็บดูข้อมูลตัวอย่างและทดลองร่างโครงการได้ทันที ไม่เกิด Error หน้าขาวแน่นอน

---

## 🚀 ขั้นตอนการติดตั้งบน Web Hosting / cPanel

### 1. อัปโหลดไฟล์ขึ้น Server
- อัปโหลดไฟล์และโฟลเดอร์ทั้งหมดนี้ ไปไว้ที่ `public_html` (หรือโฟลเดอร์ย่อย เช่น `public_html/school/`)
- ตรวจสอบว่ามีไฟล์ `index.php`, `.htaccess`, โฟลเดอร์ `config/`, `includes/`, `api/`, `database/` อยู่ครบถ้วน

### 2. นำเข้าฐานข้อมูล MySQL (ถ้าต้องการบันทึกข้อมูลจริง)
1. เปิด **phpMyAdmin** บนโฮสติ้งของคุณ
2. สร้างฐานข้อมูลใหม่ เช่น `school_budget_db` (กำหนด Collation เป็น `utf8mb4_unicode_ci`)
3. คลิกแท็บ **Import (นำเข้า)**:
   - นำเข้าไฟล์ `database/schema.sql` (สร้างตาราง)
   - นำเข้าไฟล์ `database/seed.sql` (ข้อมูลตัวอย่าง สพฐ.)
4. เปิดไฟล์ `config/database.php` แล้วแก้ไขข้อมูลการเชื่อมต่อ:
   ```php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'ชื่อฐานข้อมูลที่สร้าง');
   define('DB_USER', 'ชื่อผู้ใช้ MySQL');
   define('DB_PASS', 'รหัสผ่าน MySQL');
   ```

### 3. เปิดใช้งานผ่านเบราว์เซอร์
- เข้าชมเว็บไซต์ของคุณ เช่น:
  - `http://your-domain.com/` หรือ
  - `http://your-domain.com/school/`
- ระบบจะเข้าสู่หน้าแดชบอร์ดทันที!

---

## 🛠️ วิธีแก้ไขปัญหาเมื่อเจอข้อผิดพลาด "404 Not Found"

หากเปิดหน้าเว็บแล้วพบข้อความ:
> **404 Not Found - The resource requested could not be found on this server!**

สาเหตุหลักเกิดจากการวางตำแหน่งไฟล์บน Server ไม่ตรงกับ Path ของโดเมน กรุณาตรวจสอบตาม 4 ข้อย่อยนี้:

### 1. ย้ายไฟล์ออกมาไว้ที่ `public_html` โดยตรง (สาเหตุที่พบบ่อยที่สุด ⭐)
เมื่อแตกไฟล์ `.zip` ใน cPanel File Manager โฮสต์มักจะสร้างโฟลเดอร์ซ้อน เช่น `public_html/school-budget/`
- ส่งผลให้เมื่อเข้า `http://your-domain.com/` เซิร์ฟเวอร์จะหาไฟล์ไม่เจอ (404)
- **วิธีแก้:** ให้เข้าไปในโฟลเดอร์ที่แตกไฟล์ไว้ กด **Select All** แล้วกด **Move** ย้ายไฟล์ทั้งหมดออกมาไว้ที่ `public_html/` โดยตรง
- ตรวจสอบให้แน่ใจว่าไฟล์ `index.php`, `dashboard.php`, และ `.htaccess` อยู่ที่ `public_html/` ทันที

### 2. ทดลองเปิด URL ระบุชื่อไฟล์ตรงๆ
- ลองเปิด: `http://your-domain.com/dashboard.php` หรือ `http://your-domain.com/index.php`
- หากเปิดได้ แสดงว่าเป็นเรื่องของการตั้งค่า DirectoryIndex ในเซิร์ฟเวอร์ ให้ตรวจสอบว่ามีไฟล์ `.htaccess` ที่เราเตรียมไว้แล้วหรือไม่

### 3. เปิดการแสดงไฟล์ที่ซ่อนอยู่ (Show Hidden Files) ใน cPanel
- ไฟล์ `.htaccess` เป็นไฟล์ขึ้นต้นด้วยจุด ซึ่งบางครั้ง cPanel จะซ่อนไว้
- ใน cPanel File Manager คลิกปุ่ม **Settings** ที่มุมขวาบน -> ติ๊กถูกที่ **"Show Hidden Files (dotfiles)"** -> กด **Save**
- ตรวจสอบว่าไฟล์ `.htaccess` อยู่ในโฟลเดอร์เดียวกับ `index.php`

### 4. ตรวจสอบ Document Root ของ Subdomain หรือ Addon Domain
- หากใช้ Subdomain (เช่น `budget.yourschool.ac.th`) ให้เข้าไปดูที่เมนู **Domains** ใน cPanel ว่าช่อง **Document Root** ชี้ไปที่โฟลเดอร์ใด แล้วนำไฟล์ไปวางในโฟลเดอร์นั้น

## 🔐 ระบบล็อกอินและจัดการโรงเรียน (Multi-Tenant & Super Admin)

### สาเหตุที่เกิดขึ้นก่อนหน้านี้:
1. **มีคำสั่ง Auto Demo Login เดิม:** ในเวอร์ชันก่อนหน้า มีโค้ดล็อกอินอัตโนมัติเป็นแอดมินของโรงเรียนตัวอย่างที่ 1 เสมอ จึงทำให้เมื่อเปิดหน้าเว็บเข้ามา ระบบจะดึงข้อมูลตัวอย่างเดิมมาแสดงทันทีโดยข้ามหน้าล็อกอิน
2. **ระบบตรวจสอบชื่อผู้ใช้เดิมถูกล็อกไว้เฉพาะ Demo:** ในหน้า `login.php` เดิมมีการตรวจเช็คเฉพาะผู้ใช้ `admin`, `director`, `teacher` จึงทำให้ชื่อผู้ใช้ของโรงเรียนใหม่ล็อกอินไม่ผ่าน
3. **การบันทึกข้อมูลโรงเรียนใหม่:** หากโฮสติ้งยังไม่ได้ต่อฐานข้อมูล MySQL ข้อมูลโรงเรียนที่สร้างใหม่จะไม่ถูกบันทึกลงไฟล์ จึงทำให้โรงเรียนใหม่ไม่ปรากฏ

### สิ่งที่แก้ไขและปรับปรุงเรียบร้อยแล้ว:
1. **ตัดคำสั่ง Auto Demo Login ออก 100%:** เมื่อเปิดหน้าเว็บ ระบบจะตรวจสอบสิทธิ์และพามาที่หน้า `login.php` เสมอ
2. **หน้า Login รองรับโรงเรียนทุกแห่งในระบบ:**
   - มีแผงแสดง **รายชื่อโรงเรียนที่สร้างขึ้นมาใหม่ทั้งหมด** ให้คลิกเข้าสู่ระบบได้ทันที
   - สามารถกรอก **ชื่อผู้ใช้ (Username)** หรือ **รหัส SMIS 8 หลัก** ของโรงเรียนที่สร้างใหม่ พร้อมรหัสผ่านที่ตั้งไว้ เพื่อเข้าสู่ระบบได้ถูกต้อง 100%
   - มีระบบจดจำข้อมูลทั้งในฐานข้อมูล MySQL และไฟล์ `config/schools_data.json` ทำให้ข้อมูลโรงเรียนใหม่อยู่ถาวรแม้ยังไม่ได้ติดตั้ง MySQL
3. **ปุ่ม "เข้าใช้งาน" โดยตรงใน Super Admin:**
   - ในหน้า `super_admin.php` เมื่อสร้างโรงเรียนใหม่เสร็จ สามารถกดปุ่มสีฟ้า **"เข้าใช้งาน"** ในตาราง เพื่อสลับเข้าเป็นผู้ดูแลโรงเรียนนั้นได้ทันทีในคลิกเดียว
4. **ปุ่ม "สลับโรงเรียน" ในแถบเมนูด้านบน (Header):**
   - เมื่อมีโรงเรียนในระบบมากกว่า 1 แห่ง จะมีเมนูให้สลับไปมาระหว่างโรงเรียนต่างๆ ได้อย่างสะดวกสบาย

---

## 💻 การเข้าสู่ระบบในระบบ

- **ศูนย์ควบคุม Super Admin:** เข้าที่ `super_admin.php` หรือพิมพ์ Username: `superadmin` / Password: `super123456`
- **โรงเรียนที่สร้างใหม่:** ใช้ Username และ Password ที่กำหนดไว้ตอนสร้างใน Super Admin (หรือใช้รหัส SMIS 8 หลักเป็น Username)
- **โรงเรียนตัวอย่าง (สำหรับทดสอบระบบ):**
  - หัวหน้าแผนงาน: `admin` / รหัสผ่าน: `123456`
  - ผู้อำนวยการ: `director` / รหัสผ่าน: `123456`
  - ครูผู้รับผิดชอบ: `teacher` / รหัสผ่าน: `123456`

---

## 🤖 การใช้งานระบบ AI ร่างข้อเสนอโครงการ
1. ไปที่เมนู **"7. เขียนโครงการด้วย AI"**
2. เลือกแม่แบบโครงการยอดนิยม หรือพิมพ์ชื่อโครงการและงบประมาณที่ต้องการ
3. คลิกปุ่ม **"ตั้งค่า Gemini Key"** เพื่อใส่ Google Gemini API Key ของคุณ (ขอรับฟรีได้ที่ [Google AI Studio](https://aistudio.google.com/app/apikey))
4. กดปุ่ม **"ให้ AI ร่างข้อเสนอโครงการ สพฐ."**
5. ระบบจะร่างเอกสารฉบับสมบูรณ์ 13 หัวข้อตามระเบียบ สพฐ. พร้อมตารางงบประมาณ 4 หมวด
6. สามารถกด **"ดาวน์โหลด Word (.doc)"** หรือ **"พิมพ์ / PDF"** หรือ **"บันทึกเข้าแผนงาน"** ได้ทันที
